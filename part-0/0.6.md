New note in Single page app diagram

```mermaid
sequenceDiagram
	participant browser
	participant server
	
	%%user inputs data into form
	browser->>+server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa        content-type:application/json
	%% server saves the note to database
	server-->>-browser: 201 resource created
	%% No need for redirection we stay on the same page
	%% Javascript updates array that contains notes
	%% Javascript re-renders the notes in the DOM


```